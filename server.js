require('dotenv').config();const express=require('express'),path=require('path');const app=express();app.use(express.json());app.use(express.static(__dirname));const PORT=process.env.PORT||3000;
app.get('/api/health',(q,s)=>s.json({ok:true,providers:{alphaVantage:!!process.env.ALPHA_VANTAGE_API_KEY,openai:!!process.env.OPENAI_API_KEY,dfm:!!process.env.DFM_API_KEY,adx:!!process.env.ADX_API_KEY}}));
app.get('/api/quote',async(q,s)=>{const symbol=q.query.symbol||'RELIANCE.BSE';if(!process.env.ALPHA_VANTAGE_API_KEY)return s.json({mode:'demo',symbol,message:'Set ALPHA_VANTAGE_API_KEY in .env'});try{const u='https://www.alphavantage.co/query?function=GLOBAL_QUOTE&symbol='+encodeURIComponent(symbol)+'&apikey='+encodeURIComponent(process.env.ALPHA_VANTAGE_API_KEY);s.json(await (await fetch(u)).json())}catch(e){s.status(502).json({error:'Provider request failed'})}});
app.get('/api/portfolio',(q,s)=>s.json({currency:'INR',holdings:[]}));app.post('/api/transactions',(q,s)=>s.status(201).json({ok:true,transaction:q.body}));app.get('/api/watchlist',(q,s)=>s.json([]));app.post('/api/ai/analyze',(q,s)=>s.json({mode:process.env.OPENAI_API_KEY?'configured':'demo',answer:process.env.OPENAI_API_KEY?'AI adapter ready for your chosen OpenAI integration.':'Add OPENAI_API_KEY to .env on the server.'}));app.listen(PORT,()=>console.log('PortfolioOS: http://localhost:'+PORT));
